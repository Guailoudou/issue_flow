import { Flag, GitCommitHorizontal, LayoutDashboard, Settings, Tag, Users, Webhook } from 'lucide-react';
import { NavLink, Outlet } from 'react-router-dom';
const items = [{ to: '/admin', end: true, label: '概览', icon: LayoutDashboard }, { to: '/admin/users', label: '用户', icon: Users }, { to: '/admin/labels', label: '标签', icon: Tag }, { to: '/admin/commit-actions', label: '提交操作', icon: GitCommitHorizontal }, { to: '/admin/milestones', label: '里程碑', icon: Flag }, { to: '/admin/yunxiao', label: '云效联动', icon: Webhook }, { to: '/admin/settings', label: '平台设置', icon: Settings }];
export function AdminShell() { return <div className="page-container"><div className="mb-5"><h1 className="text-2xl font-bold">管理后台</h1><p className="mt-1 text-sm text-slate-600">管理平台、账号和公共分类数据</p></div><div className="grid items-start gap-5 lg:grid-cols-[220px_minmax(0,1fr)]"><nav className="surface flex gap-1 overflow-x-auto p-2 lg:flex-col" aria-label="管理导航">{items.map(({ to, end, label, icon: Icon }) => <NavLink key={to} to={to} end={end} className={({ isActive }) => `flex min-h-11 shrink-0 items-center gap-2 rounded-lg px-3 text-sm font-medium transition-colors ${isActive ? 'bg-brand-100 text-brand-900' : 'text-slate-600 hover:bg-slate-100'}`}><Icon className="size-4" />{label}</NavLink>)}</nav><section className="min-w-0"><Outlet /></section></div></div>; }
