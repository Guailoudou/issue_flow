# IssueFlow 简易使用说明

## 1. 登录与注册

- 已有账号：打开系统登录页，输入用户名和密码登录。
- 注册账号：在注册页填写用户名、显示名称、邮箱、密码和邀请码。
- 注册邀请码由部署环境变量 `REGISTRATION_INVITE_CODE` 配置。
- 管理员可以在“管理后台 → 用户”中创建、停用用户或重置密码。

## 2. 个人中心

登录后点击顶部头像或主导航中的“个人中心”：

- “个人资料”可以单独修改显示名称；用户名只能由管理员修改。
- “修改密码”需要填写当前密码、新密码并再次确认新密码。
- 密码修改成功后，当前账号的全部登录会话和 API Token 都会失效，需要使用新密码重新登录。
- 管理员可在“管理后台 → 用户”中修改其他用户的用户名。用户名修改后，该用户下次登录需要使用新用户名。

## 3. Issue 基本操作

进入“Issues”页面后，可以：

- 新建 Issue，填写标题、描述、负责人、多个标签和里程碑；标签支持搜索。
- 按关键词、状态、作者、负责人、一个或多个标签、里程碑筛选；组合标签要求 Issue 同时包含所选标签。
- 在 Issue 详情页编辑内容、发表评论、上传附件和调整状态。
- 如果创建时未选择标签且管理员已启用 AI 自动标签，系统会在后台队列中根据标题和正文自动选择现有标签；附件和图片不会发送给 AI，创建 Issue 无需等待 AI 返回。

Issue 有两种状态：

- **开放**：正在处理。
- **已关闭**：已经完成并关闭。

## 4. 附件

- 支持上传任意文件类型。
- 可以把本地文件或图片直接拖入附件区域。
- 也可以点击或使用键盘聚焦附件区域，然后按 `Ctrl+V` / `⌘V` 粘贴剪贴板中的文件或截图。
- 拖拽或粘贴的内容会先加入待上传列表，确认创建 Issue 或点击“上传附件”后才会上传。
- 单个附件最大 10 MiB。
- 每个 Issue 最多上传 20 个附件。
- PNG、JPEG、GIF、WebP 图片可直接预览，其他文件通过下载查看。

## 5. 导出表格

在 Issues 页面点击“导出表格”，选择已关闭 Issue 的关闭时间范围后导出：

- 开放 Issue 不受时间范围限制，始终参与导出。
- 已关闭 Issue 仅导出关闭时间位于所选范围内的数据。
- 当前筛选条件会同时应用到导出结果。

## 6. 云效联动

管理员进入“管理后台 → 云效联动”，填写云效版本、OpenAPI 地址、组织 ID、仓库 ID、仓库页面地址、访问令牌和 Webhook Secret，然后保存并启用联动。

在云效仓库创建 Webhook 时：

- URL 使用页面显示的 IssueFlow Webhook 地址，格式为 `https://你的域名/api/integrations/yunxiao/webhook`。
- Secret/Token 必须与 IssueFlow 中保存的 Webhook Secret 相同。
- 开启 Push Hook 和 Merge Request Hook 事件。

管理员可以在“管理后台 → 提交操作”定义指令，每个指令可以修改状态、添加标签，或同时执行两者。系统默认提供：

| 标记 | 作用 |
| --- | --- |
| `#4` | 关联 4 号 Issue，不改变状态 |
| `#c4` | 关联 4 号 Issue，并关闭 |
| `#o4` | 关联 4 号 Issue，并重新打开 |

示例：

```bash
git commit -m "完成登录页面 #c4"
git push
```

同一个提交可以操作多个 Issue，例如：

```text
完成本轮修复 #c4 #bug7
```

自定义指令的格式为 `#关键字Issue编号`。同一提交可以组合多个指令；多个状态指令作用于同一个 Issue 时，以最后出现的状态指令为准，标签会合并添加且不会重复。

## 7. Docker 部署与更新

首次部署前复制并修改 Docker 环境配置，确保管理员密码、邀请码、云效加密密钥和数据卷配置正确。启动或更新服务：

```bash
docker compose --env-file .env.docker up -d --build --force-recreate
```

查看服务状态：

```bash
docker compose --env-file .env.docker ps
```

更新后如页面仍显示旧内容，请清除域名对应的 `/index.html`、`/issues` 和已访问过的 `/issues/{编号}` 页面缓存，再确认概览页显示的前后端版本。

## 8. API Token

登录后进入“API Token”页面（`/settings/api-tokens`），可以创建用于脚本、自动化和外部系统调用 API 的个人 Token。Token 继承当前账户权限，完整值只显示一次，请立即复制并安全保存。

调用时使用标准 Bearer 请求头：

```bash
curl -sS https://issue.gldhn.top/api/auth/me \
  -H "Authorization: Bearer $ISSUEFLOW_API_TOKEN"
```

接口、请求参数、错误码和安全建议参见 [IssueFlow API 使用说明](./API使用说明.md)。

## 9. 全量数据备份与恢复

管理员进入“管理后台 → 数据备份”后可以下载全量 JSON 备份。文件包含全部用户、Issue、评论、标签、配置、API Token 哈希、加密凭据和附件，请将其作为敏感文件妥善保管。

导入会直接覆盖当前平台全部数据且无法撤销。操作前请先导出当前数据，在页面选择备份文件并输入“覆盖全部数据”确认。跨服务器恢复时还需要沿用原来的 `YUNXIAO_ENCRYPTION_KEY`；导入完成后请重新载入页面。
